import { sql } from 'drizzle-orm'
import { boolean, index, integer, jsonb, pgTable, text, timestamp, uniqueIndex } from 'drizzle-orm/pg-core'

export const user = pgTable('user', { id: text('id').primaryKey(), name: text('name').notNull(), email: text('email').notNull().unique(), emailVerified: boolean('emailVerified').notNull().default(false), image: text('image'), role: text('role').notNull().default('user'), createdAt: timestamp('createdAt').notNull().defaultNow(), updatedAt: timestamp('updatedAt').notNull().defaultNow() })
export const session = pgTable('session', { id: text('id').primaryKey(), expiresAt: timestamp('expiresAt').notNull(), token: text('token').notNull().unique(), createdAt: timestamp('createdAt').notNull().defaultNow(), updatedAt: timestamp('updatedAt').notNull().defaultNow(), ipAddress: text('ipAddress'), userAgent: text('userAgent'), userId: text('userId').notNull() })
export const account = pgTable('account', { id: text('id').primaryKey(), accountId: text('accountId').notNull(), providerId: text('providerId').notNull(), userId: text('userId').notNull(), accessToken: text('accessToken'), refreshToken: text('refreshToken'), idToken: text('idToken'), accessTokenExpiresAt: timestamp('accessTokenExpiresAt'), refreshTokenExpiresAt: timestamp('refreshTokenExpiresAt'), scope: text('scope'), password: text('password'), createdAt: timestamp('createdAt').notNull().defaultNow(), updatedAt: timestamp('updatedAt').notNull().defaultNow() }, (table) => [uniqueIndex('account_provider_account_unique').on(table.providerId, table.accountId)])
export const verification = pgTable('verification', { id: text('id').primaryKey(), identifier: text('identifier').notNull(), value: text('value').notNull(), expiresAt: timestamp('expiresAt').notNull(), createdAt: timestamp('createdAt').notNull().defaultNow(), updatedAt: timestamp('updatedAt').notNull().defaultNow() })
export const subscriptionPlans = pgTable('subscription_plans', { id: text('id').primaryKey(), name: text('name').notNull().unique(), description: text('description').notNull().default(''), rank: integer('rank').notNull().default(0), isDefault: boolean('isDefault').notNull().default(false), rateLimit: integer('rateLimit').notNull().default(60), dailyLimit: integer('dailyLimit').notNull().default(-1), weeklyLimit: integer('weeklyLimit').notNull().default(-1), monthlyLimit: integer('monthlyLimit').notNull().default(-1), createdAt: timestamp('createdAt').notNull().defaultNow(), updatedAt: timestamp('updatedAt').notNull().defaultNow() })
export const planEntitlements = pgTable('plan_entitlements', { id: text('id').primaryKey(), userId: text('userId').notNull(), planId: text('planId').notNull(), subscriptionId: text('subscriptionId'), startsAt: timestamp('startsAt').notNull().defaultNow(), expiresAt: timestamp('expiresAt'), source: text('source').notNull().default('default'), createdAt: timestamp('createdAt').notNull().defaultNow() })
export const redeemCodes = pgTable('redeem_codes', { id: text('id').primaryKey(), code: text('code').notNull().unique(), kind: text('kind').notNull().default('plan'), planId: text('planId'), credits: integer('credits'), durationDays: integer('durationDays').notNull().default(30), durationValue: integer('durationValue').notNull().default(30), durationUnit: text('durationUnit').notNull().default('day'), expiresAt: timestamp('expiresAt'), redeemedAt: timestamp('redeemedAt'), redeemedBy: text('redeemedBy'), orderId: text('orderId'), subscriptionId: text('subscriptionId'), createdAt: timestamp('createdAt').notNull().defaultNow() })
export const creditGrants = pgTable('credit_grants', { id: text('id').primaryKey(), userId: text('userId').notNull(), credits: integer('credits').notNull(), remainingCredits: integer('remainingCredits').notNull(), expiresAt: timestamp('expiresAt'), source: text('source').notNull().default('redeem'), redeemCodeId: text('redeemCodeId'), createdAt: timestamp('createdAt').notNull().defaultNow() })
export const creditTransactions = pgTable('credit_transactions', { id: text('id').primaryKey(), userId: text('userId').notNull(), creditGrantId: text('creditGrantId').notNull(), delta: integer('delta').notNull(), balanceAfter: integer('balanceAfter').notNull(), reason: text('reason').notNull(), referenceId: text('referenceId'), createdAt: timestamp('createdAt').notNull().defaultNow() }, (table) => [index('credit_transactions_user_created_idx').on(table.userId, table.createdAt), index('credit_transactions_grant_created_idx').on(table.creditGrantId, table.createdAt)])
export const apiUsage = pgTable('api_usage', { id: text('id').primaryKey(), userId: text('userId').notNull(), planId: text('planId').notNull(), usageDate: text('usageDate').notNull(), requestCount: integer('requestCount').notNull().default(0), windowStartedAt: timestamp('windowStartedAt').notNull().defaultNow(), windowCount: integer('windowCount').notNull().default(0) })
export const appSettings = pgTable('app_settings', { key: text('key').primaryKey(), value: text('value').notNull(), updatedAt: timestamp('updatedAt').notNull().defaultNow() })
export const paymentProviders = pgTable('payment_providers', { id: text('id').primaryKey(), code: text('code').notNull().unique(), name: text('name').notNull(), enabled: boolean('enabled').notNull().default(true), createdAt: timestamp('createdAt').notNull().defaultNow(), updatedAt: timestamp('updatedAt').notNull().defaultNow() })
export const skus = pgTable('skus', { id: text('id').primaryKey(), code: text('code').notNull().unique(), name: text('name').notNull(), description: text('description').notNull().default(''), kind: text('kind').notNull(), planId: text('planId'), credits: integer('credits'), durationValue: integer('durationValue').notNull().default(1), durationUnit: text('durationUnit').notNull().default('month'), active: boolean('active').notNull().default(true), createdAt: timestamp('createdAt').notNull().defaultNow(), updatedAt: timestamp('updatedAt').notNull().defaultNow() })
export const providerOfferMappings = pgTable('provider_offer_mappings', { id: text('id').primaryKey(), providerId: text('providerId').notNull(), externalOfferType: text('externalOfferType').notNull(), externalOfferId: text('externalOfferId').notNull(), externalName: text('externalName').notNull().default(''), skuId: text('skuId').notNull(), unitsPerItem: integer('unitsPerItem').notNull().default(1), enabled: boolean('enabled').notNull().default(true), createdAt: timestamp('createdAt').notNull().defaultNow(), updatedAt: timestamp('updatedAt').notNull().defaultNow() }, (table) => [uniqueIndex('provider_offer_mappings_offer_unique').on(table.providerId, table.externalOfferType, table.externalOfferId)])
export const orders = pgTable('orders', { id: text('id').primaryKey(), providerId: text('providerId'), externalOrderId: text('externalOrderId'), externalCustomerId: text('externalCustomerId'), externalOfferId: text('externalOfferId'), externalOfferTitle: text('externalOfferTitle').notNull().default(''), userId: text('userId'), skuId: text('skuId'), status: text('status').notNull().default('pending'), termMonths: integer('termMonths').notNull().default(1), amount: text('amount').notNull().default(''), currency: text('currency').notNull().default('CNY'), deliveryStatus: text('deliveryStatus').notNull().default('pending'), deliveryAttempts: integer('deliveryAttempts').notNull().default(0), deliveryAttemptedAt: timestamp('deliveryAttemptedAt'), deliveredAt: timestamp('deliveredAt'), deliveryLastError: text('deliveryLastError'), createdAt: timestamp('createdAt').notNull().defaultNow(), updatedAt: timestamp('updatedAt').notNull().defaultNow() }, (table) => [uniqueIndex('orders_provider_external_unique').on(table.providerId, table.externalOrderId)])
export const payments = pgTable('payments', { id: text('id').primaryKey(), orderId: text('orderId').notNull(), providerId: text('providerId'), externalPaymentId: text('externalPaymentId'), status: text('status').notNull(), amount: text('amount').notNull().default(''), currency: text('currency').notNull().default('CNY'), paidAt: timestamp('paidAt'), createdAt: timestamp('createdAt').notNull().defaultNow(), updatedAt: timestamp('updatedAt').notNull().defaultNow() })
export const subscriptions = pgTable('subscriptions', { id: text('id').primaryKey(), userId: text('userId'), planId: text('planId').notNull(), skuId: text('skuId'), providerId: text('providerId'), providerSubscriptionId: text('providerSubscriptionId'), status: text('status').notNull().default('pending_activation'), currentPeriodStart: timestamp('currentPeriodStart'), currentPeriodEnd: timestamp('currentPeriodEnd'), cancelAtPeriodEnd: boolean('cancelAtPeriodEnd').notNull().default(false), canceledAt: timestamp('canceledAt'), endedAt: timestamp('endedAt'), createdAt: timestamp('createdAt').notNull().defaultNow(), updatedAt: timestamp('updatedAt').notNull().defaultNow() })
export const subscriptionEvents = pgTable('subscription_events', { id: text('id').primaryKey(), subscriptionId: text('subscriptionId').notNull(), type: text('type').notNull(), fromStatus: text('fromStatus'), toStatus: text('toStatus').notNull(), providerEventId: text('providerEventId'), detail: text('detail').notNull().default(''), createdAt: timestamp('createdAt').notNull().defaultNow() })
export const providerEvents = pgTable('provider_events', { id: text('id').primaryKey(), providerId: text('providerId').notNull(), externalEventId: text('externalEventId').notNull(), type: text('type').notNull(), status: text('status').notNull().default('received'), error: text('error'), rawPayload: text('rawPayload').notNull(), processedAt: timestamp('processedAt'), createdAt: timestamp('createdAt').notNull().defaultNow() }, (table) => [uniqueIndex('provider_events_external_unique').on(table.providerId, table.externalEventId, table.type)])
export const activityLogs = pgTable('activity_logs', { id: text('id').primaryKey(), actorId: text('actorId'), action: text('action').notNull(), resourceType: text('resourceType').notNull(), resourceId: text('resourceId'), detail: text('detail').notNull().default(''), createdAt: timestamp('createdAt').notNull().defaultNow() })
export const apiKeys = pgTable('api_keys', { id: text('id').primaryKey(), userId: text('userId').notNull(), name: text('name').notNull(), kind: text('kind').notNull().default('custom'), prefix: text('prefix').notNull(), keyHash: text('keyHash').notNull(), secretEncrypted: text('secretEncrypted'), serviceScopes: jsonb('serviceScopes').$type<string[]>().notNull().default(['*']), expiresAt: timestamp('expiresAt'), lastUsedAt: timestamp('lastUsedAt'), revokedAt: timestamp('revokedAt'), createdAt: timestamp('createdAt').notNull().defaultNow(), updatedAt: timestamp('updatedAt').notNull().defaultNow() }, (table) => [uniqueIndex('api_keys_hash_unique').on(table.keyHash), uniqueIndex('api_keys_user_default_unique').on(table.userId).where(sql`${table.kind} = 'default' AND ${table.revokedAt} IS NULL`), index('api_keys_user_created_idx').on(table.userId, table.createdAt)])
export const apiServices = pgTable('api_services', { id: text('id').primaryKey(), code: text('code').notNull().unique(), name: text('name').notNull(), description: text('description').notNull().default(''), transport: text('transport').notNull().default('http'), bindingName: text('bindingName'), baseUrl: text('baseUrl').notNull(), authType: text('authType').notNull().default('none'), authConfigEncrypted: text('authConfigEncrypted'), requiredPermissionId: text('requiredPermissionId'), enabled: boolean('enabled').notNull().default(true), createdAt: timestamp('createdAt').notNull().defaultNow(), updatedAt: timestamp('updatedAt').notNull().defaultNow() }, (table) => [index('api_services_permission_idx').on(table.requiredPermissionId)])
export const serviceApis = pgTable('service_apis', { id: text('id').primaryKey(), serviceId: text('serviceId').notNull(), code: text('code').notNull(), name: text('name').notNull(), description: text('description').notNull().default(''), path: text('path').notNull(), method: text('method').notNull(), parameters: jsonb('parameters').$type<Array<{ name: string; location: 'query' | 'header' | 'path' | 'body'; dataType: 'string' | 'number' | 'boolean' | 'json'; required: boolean; description: string }>>().notNull().default([]), usageUnits: integer('usageUnits').notNull().default(1), timeoutMs: integer('timeoutMs').notNull().default(30000), enabled: boolean('enabled').notNull().default(true), createdAt: timestamp('createdAt').notNull().defaultNow(), updatedAt: timestamp('updatedAt').notNull().defaultNow() }, (table) => [uniqueIndex('service_apis_service_code_unique').on(table.serviceId, table.code), index('service_apis_service_idx').on(table.serviceId)])
export const permissions = pgTable('permissions', { id: text('id').primaryKey(), code: text('code').notNull().unique(), name: text('name').notNull(), description: text('description').notNull().default(''), createdAt: timestamp('createdAt').notNull().defaultNow(), updatedAt: timestamp('updatedAt').notNull().defaultNow() })
export const planPermissionGrants = pgTable('plan_permission_grants', { id: text('id').primaryKey(), planId: text('planId').notNull(), permissionId: text('permissionId').notNull(), createdAt: timestamp('createdAt').notNull().defaultNow() }, (table) => [uniqueIndex('plan_permission_grants_unique').on(table.planId, table.permissionId), index('plan_permission_grants_plan_idx').on(table.planId), index('plan_permission_grants_permission_idx').on(table.permissionId)])
export const allTables = { user, session, account, verification, subscriptionPlans, planEntitlements, redeemCodes, creditGrants, creditTransactions, apiUsage, appSettings, paymentProviders, skus, providerOfferMappings, orders, payments, subscriptions, subscriptionEvents, providerEvents, activityLogs, apiKeys, apiServices, serviceApis, permissions, planPermissionGrants }
